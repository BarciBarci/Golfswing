# Android wrapper for the Golf Swing Analysis PWA.
#
# Version of the Android app. F-Droid reads these two values to decide when a
# new release is available (and to check update tags), so bump them together
# and tag the release in git (see android/README.md).
VERSION_CODE=1
VERSION_NAME=1.0.0

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
